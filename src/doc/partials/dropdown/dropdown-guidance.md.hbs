* The following theme classes can be applied to the `<div class="dropdown">` tag: `.theme-default`, `.theme-alt`, `.theme-light` or `.theme-dark`.
* The `disabled` attribute can be applied to the `<button>` tag to disable the control or the `.disabled` class can be applied to an `<a>` tag to disable an item in the dropdown menu.
