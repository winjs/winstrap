* The `data-theme` attribute can be set to: `theme-default`, `theme-alt`, `theme-light` or `theme-dark`. It is applied to the `<a>` tag.
* The `data-placement` attribute can be set to `top`, `right`, `bottom` or `left` and governs the placement of the flyout with respect to the entry point. It is applied to the `<a>` tag. The default value is `right`.