* In addition to those options listed above for Push Buttons, the following theme classes can be applied to the `<a>` tag: `.theme-default`, `.theme-alt`, `.theme-light` or `.theme-dark`.
* Use the `.btn-block` class on the `<a>` tag to make it full width and stacked.
