* The following theme classes can be applied to the `<form>` and `<button>` tags: `.theme-dark` or `.theme-alt`.
* The `disabled` attribute can be applied to the `<input>` tags for Email, Password and File input. 
