* The following theme classes can be applied to the `<form>` tag: `.theme-default`, `.theme-light`, `.theme-dark` or `.theme-alt`.
* The `disabled` attribute can be applied to the `<select>` tags above.